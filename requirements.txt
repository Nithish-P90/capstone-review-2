# NVD ingestion (teammates)
nvdlib
python-dotenv
qdrant-client
sentence-transformers

# Security dataset preprocessing (this module)
requests
tqdm
datasets
huggingface_hub

# AST parsing + pipeline agents + dashboard
tree-sitter>=0.22.0
tree-sitter-c>=0.22.0
rich>=13.0.0
